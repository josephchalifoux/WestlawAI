{
  "name": "pleadings-pro-bootstrap",
  "private": true,
  "version": "0.0.6",
  "engines": { "node": ">=18" },
  "scripts": {
    "prebuild": "node -e \"require('fs').mkdirSync('.gen',{recursive:true}); require('fs').writeFileSync('.gen/start.js', process.env.BOOTSTRAP_JS);\"",
    "build": "node -e \"const fs=require('fs');['app','apps/web/app','./app','./apps/web/app'].forEach(p=>{try{fs.rmSync(p,{recursive:true,force:true});console.log('Removed',p);}catch{}});\" && node .gen/start.js && npm --prefix apps/web run build",
    "start": "npm --prefix apps/web run start"
  },
  "dependencies": {},
  "devDependencies": {}
}
